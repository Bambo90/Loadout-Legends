/* ITEM FILE: Enhancement Grip Wraps
 * Contains concrete item defs (body/aura, optional dropWeight).
 * ilvl/tier/roll ranges are handled upstream by affix/generator systems.
 */

const ITEMS_ENHANCEMENTS_GRIP_WRAPS = [];

if (typeof window !== "undefined") {
    window.ITEMS_ENHANCEMENTS_GRIP_WRAPS = ITEMS_ENHANCEMENTS_GRIP_WRAPS;
}
